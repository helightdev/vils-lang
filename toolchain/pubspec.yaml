name: vils_toolchain
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.3

resolution: workspace

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0
  antlr4: ^4.13.2
  graphs: ^2.3.2
  dart_scope_functions: ^1.0.6
  collection: ^1.19.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
